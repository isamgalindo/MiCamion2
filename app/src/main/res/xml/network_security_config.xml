<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Add specific domains that are allowed to be accessed via cleartext -->
        <!-- For example, if your server is "example.com" -->
        <domain includeSubdomains="true">192.168.128.13</domain>
    </domain-config>
</network-security-config>